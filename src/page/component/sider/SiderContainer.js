import React from 'react';
import SiderPresenter from './SiderPresenter';

const SiderContainer = (props) => {
  return <SiderPresenter props={props}></SiderPresenter>;
};

export default SiderContainer;
